---
const navItems = [
  { href: '/about', label: 'About' },
  { href: '/contact', label: 'Work' },
  { href: '/projects', label: 'Get in touch' },
]
---

<nav class="nav-bar">
    <ul class="nav-list">
        <li>
            <a href="/">JUSTIN LEE</a>
        </li>
        {navItems.map(item => (
            <li class="nav-item">
                <a class="nav-link" href={item.href}>{item.label}</a>
            </li>
        ))}
    </ul>
</nav>